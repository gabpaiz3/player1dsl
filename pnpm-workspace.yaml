packages:
  - 'packages/*'

allowBuilds:
  esbuild: set this to true or false

# esbuild ships a platform binary and needs its postinstall to place it.
# pnpm blocks build scripts by default; this approves that one explicitly.
onlyBuiltDependencies:
  - esbuild
